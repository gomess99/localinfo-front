import React from 'react'
import ReactDOM from 'react-dom'
import App from './App' // Importe o componente 'App' sem a extensão .jsx
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
